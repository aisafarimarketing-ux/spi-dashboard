# SPI environment variables.
#
# Copy to `.env.local` (gitignored) and fill in. Never commit real values.

# ── Database ────────────────────────────────────────────────────────────────
# Railway Postgres connection string. Get this from the Railway dashboard:
#   Project → Postgres service → Connect → Postgres Connection URL.
# Looks like: postgresql://postgres:PASSWORD@HOST.railway.app:PORT/railway
DATABASE_URL=

# ── Clerk (auth + organizations) ────────────────────────────────────────────
# From Clerk dashboard → API keys.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Where Clerk redirects on sign-in / sign-up — leave defaults unless you've
# customised the routes.
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# ── Anthropic (Tell-SPI parser) — added in Unit 1.9 ─────────────────────────
# ANTHROPIC_API_KEY=
